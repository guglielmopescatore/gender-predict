# Core dependencies
numpy>=1.20.0
pandas>=1.3.0
torch>=1.10.0
scikit-learn>=1.0.0
matplotlib>=3.4.0
seaborn>=0.11.0
tqdm>=4.62.0

# Optional for equalized odds in postprocessing (uncommend if needed)
# fairlearn>=0.7.0

# Transliteration of non-Latin scripts (pure Python)
regex>=2021.8.3
pypinyin>=0.44.0
hangul-romanize>=0.1.0
transliterate>=1.10.2

# Japanese kana romanisation is optional (needs a C build): pip install -e ".[ja]"
